// object types

// setting the types the the object contains - key value types
// const person: {
//     name: string;
//     age: number;
// }

//using infered types is better when working with objects
const person = {
    name: 'Alex',
    age: 20
};

console.log(person.name)
;